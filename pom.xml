<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>in.vidyalai</groupId>
    <artifactId>claude-agent-sdk-parent</artifactId>
    <version>${revision}</version>
    <packaging>pom</packaging>

    <name>Claude Agent SDK Java - Parent</name>
    <description>Parent POM for Claude Agent Java SDK and Examples</description>
    <url>https://github.com/vidyalai-in/claude-agent-sdk-java</url>

    <licenses>
        <license>
            <name>MIT License</name>
            <url>https://opensource.org/licenses/MIT</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Amit Padalkar</name>
            <organization>Vidyalai</organization>
            <organizationUrl>https://vidyalai.in</organizationUrl>
        </developer>
    </developers>

    <modules>
        <module>sdk</module>
        <module>examples</module>
    </modules>

    <properties>
        <!-- CI Friendly Versions - Define version once here -->
        <revision>0.2.0</revision>

        <!-- Baseline: Java 17 (LTS). `release` - not source/target - is what
             actually rejects post-17 JDK APIs at compile time; source/target
             would happily link against a newer rt image and then fail at
             runtime on a 17 JRE. Virtual threads are still used when the
             runtime provides them; see internal/Threads.java. -->
        <maven.compiler.release>17</maven.compiler.release>
        <maven.compiler.plugin.version>3.14.1</maven.compiler.plugin.version>
        <maven.surefire.plugin.version>3.5.4</maven.surefire.plugin.version>
        <maven.jar.plugin.version>3.5.0</maven.jar.plugin.version>
        <maven.dependency.plugin.version>3.9.0</maven.dependency.plugin.version>
        <maven.build.helper.plugin.version>3.6.1</maven.build.helper.plugin.version>
        <maven.exec.plugin.version>3.6.3</maven.exec.plugin.version>
        <maven.templating.plugin.version>3.1.0</maven.templating.plugin.version>
        <maven.javadoc.plugin.version>3.12.0</maven.javadoc.plugin.version>
        <maven.source.plugin.version>3.4.0</maven.source.plugin.version>
        <maven.flatten.plugin.version>1.7.3</maven.flatten.plugin.version>
        <maven.gpg.plugin.version>3.2.8</maven.gpg.plugin.version>
        <maven.central.publishing.plugin.version>0.11.0</maven.central.publishing.plugin.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Only the sdk module is published. The parent POM does not need to be
             deployed because flatten-maven-plugin (ossrh mode) inlines it into the
             sdk POM, and the examples module is reference code, not a library.
             sdk/pom.xml overrides this back to false. -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <!-- json-schema-validator 3.x is the newest release line, but it is
             built against Jackson 3 (the `tools.jackson` coordinates). This SDK
             is on Jackson 2, so 3.x would add a second, complete JSON stack to
             every consumer's classpath. 2.0.4 is the newest release on the
             Jackson-2 line and reuses the databind we already ship. -->
        <json.schema.validator.version>2.0.4</json.schema.validator.version>
        <jackson.databind.version>2.21.0</jackson.databind.version>
        <jackson.annotations.version>2.21</jackson.annotations.version>
        <jspecify.version>1.0.0</jspecify.version>
        <junit.version>6.0.2</junit.version>
        <assertj.version>3.27.7</assertj.version>
        <mockito.version>5.21.0</mockito.version>
        <opentelemetry.version>1.61.0</opentelemetry.version>
    </properties>

    <scm child.scm.connection.inherit.append.path="false"
         child.scm.developerConnection.inherit.append.path="false"
         child.scm.url.inherit.append.path="false">
        <connection>scm:git:git://github.com/vidyalai-in/claude-agent-sdk-java.git</connection>
        <developerConnection>scm:git:ssh://github.com/vidyalai-in/claude-agent-sdk-java.git</developerConnection>
        <url>https://github.com/vidyalai-in/claude-agent-sdk-java</url>
        <tag>HEAD</tag>
    </scm>

    <dependencyManagement>
        <dependencies>
            <!-- JSON serialization -->
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.databind.version}</version>
            </dependency>
            <dependency>
                <groupId>com.networknt</groupId>
                <artifactId>json-schema-validator</artifactId>
                <version>${json.schema.validator.version}</version>
                <exclusions>
                    <!-- Declared compile-scope by mistake upstream: a Surefire
                         report formatter has no business on a runtime path. -->
                    <exclusion>
                        <groupId>me.fabriciorby</groupId>
                        <artifactId>maven-surefire-junit5-tree-reporter</artifactId>
                    </exclusion>
                    <!-- Only needed to read schemas written as YAML. Tool input
                         schemas reach us as already-parsed maps. -->
                    <exclusion>
                        <groupId>com.fasterxml.jackson.dataformat</groupId>
                        <artifactId>jackson-dataformat-yaml</artifactId>
                    </exclusion>
                </exclusions>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.annotations.version}</version>
            </dependency>

            <!-- Nullable annotations -->
            <dependency>
                <groupId>org.jspecify</groupId>
                <artifactId>jspecify</artifactId>
                <version>${jspecify.version}</version>
            </dependency>

            <!-- Testing -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
            </dependency>

            <!-- OpenTelemetry — test-scope only. The SDK uses reflection to
                 inject W3C trace context, so this dep is never present at
                 runtime unless the consumer adds it themselves. -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-api</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-sdk</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-context</artifactId>
                <version>${opentelemetry.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.plugin.version}</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <compilerArgs>
                            <arg>-parameters</arg>
                        </compilerArgs>
                        <!-- useIncrementalCompilation is left at its default (true),
                             which recompiles the whole module whenever anything
                             changed. Setting it to false does NOT disable
                             incremental compilation - the flag is inverted. It
                             switches the plugin to timestamp-based stale-source
                             selection, which hands javac a partial source set and
                             leans on implicit compilation for the rest. That is
                             unsound for Java: a file whose own timestamp did not
                             move is never recompiled even when a type it depends
                             on changed, and any class outside the reference
                             closure of the "stale" files is simply never written.
                             It produced intermittent "cannot find symbol" failures
                             here, and `SdkVersion.java` made it constant, because
                             templating-maven-plugin regenerates it every run so it
                             was always the one stale file. -->
                        <!-- Run javac in its own process, so a large compile cannot
                             exhaust the Maven JVM's heap. -->
                        <fork>true</fork>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven.surefire.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven.jar.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven.dependency.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>${maven.build.helper.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${maven.exec.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>templating-maven-plugin</artifactId>
                    <version>${maven.templating.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven.javadoc.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven.source.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>${maven.flatten.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-gpg-plugin</artifactId>
                    <version>${maven.gpg.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.sonatype.central</groupId>
                    <artifactId>central-publishing-maven-plugin</artifactId>
                    <version>${maven.central.publishing.plugin.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <!-- Flatten POM to resolve ${revision} property before deployment -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>flatten-maven-plugin</artifactId>
                <configuration>
                    <updatePomFile>true</updatePomFile>
                    <flattenMode>ossrh</flattenMode>
                </configuration>
                <executions>
                    <execution>
                        <id>flatten</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>flatten</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>flatten.clean</id>
                        <phase>clean</phase>
                        <goals>
                            <goal>clean</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
